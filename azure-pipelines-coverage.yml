# Python package
# Create and test a Python package on multiple Python versions.
# Add steps that analyze code, save the dist with the build record, publish to a PyPI-compatible index, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/python

trigger:
- master

jobs:
- job: 'Coberage'
  pool:
    vmImage: 'ubuntu-latest'
  strategy:
    matrix:
      Python36:
        python.version: '3.6'

  steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '$(python.version)'
    displayName: 'Use Python $(python.version)'

  - script: |
      python -m pip install --retries 3 -U pip setuptools setuptools-scm
      pip install --retries 3 -r requirements.txt
    displayName: 'Install dependencies'

  - script: |
      git submodule init
      git submodule update
      CFLAGS="" python setup.py build_ext -i
    displayName: 'Compile package'

  - script: |
      pip install pytest pytest-azurepipelines pytest-cov
      PYTHONPATH=. python -m pytest --junitxml=junit/test-results.xml --cov=cat4py --cov-report=xml --cov-report=html tests
    displayName: 'pytest'
    
  - task: PublishTestResults@2
    inputs:
      testResultsFiles: '**/test-results.xml'
      testRunTitle: 'Python $(python.version)'
    condition: succeededOrFailed()

  - script: |
      bash <(curl https://codecov.io/bash) -t $CODECOV_TOKEN -f coverage.xml
    displayName: 'codecov'
    
  - task: PublishCodeCoverageResults@1
    inputs:
      codeCoverageTool: Cobertura
      summaryFileLocation: '$(System.DefaultWorkingDirectory)/**/coverage.xml'
      reportDirectory: '$(System.DefaultWorkingDirectory)/**/htmlcov'

- job: 'Publish'
  dependsOn: 'Coverage'
  pool:
    vmImage: 'ubuntu-latest'